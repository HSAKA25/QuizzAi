<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet for Firebase / ML Kit download -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Exact alarm for daily reminder -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quizz">

        <!-- ML Kit Smart Reply -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="smart_reply"/>

        <!-- ============================= -->
        <!-- Quiz Activities -->
        <!-- ============================= -->
        <activity android:name=".QuizActivity" android:exported="false"/>
        <activity android:name=".Python" android:exported="false"/>
        <activity android:name=".C" android:exported="false"/>
        <activity android:name=".Cpp" android:exported="false"/>
        <activity android:name=".java" android:exported="false"/>
        <activity android:name=".kotlin" android:exported="false"/>
        <activity android:name=".csharp" android:exported="false"/>

        <!-- ============================= -->
        <!-- Result / Pages -->
        <!-- ============================= -->
        <activity android:name=".result" android:exported="false"/>
        <activity android:name=".page2" android:exported="false"/>
        <activity android:name=".page3" android:exported="false"/>
        <activity android:name=".difficulty_selection" android:exported="false"/>

        <!-- ============================= -->
        <!-- Analytics & Leaderboard -->
        <!-- ============================= -->
        <activity android:name=".LeaderboardActivity" android:exported="false"/>
        <activity android:name=".AnalyticsActivity" android:exported="false"/>
        <activity android:name=".Historyactivity" />
        <activity android:name=".Achievementsactivity" />

        <!-- ============================= -->
        <!-- Chatbot Activity -->
        <!-- ============================= -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <!-- Daily Reminder Receiver -->
        <receiver
            android:name=".DailyReminderReceiver"
            android:exported="false"/>

        <!-- Daily Quote Receiver -->
        <receiver
            android:name=".QuoteReceiver"
            android:exported="false"/>

        <!-- ============================= -->
        <!-- MAIN LAUNCHER -->
        <!-- ============================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>

</manifest>